#include <bits/stdc++.h>
using namespace std;

void solve() {
  string s1, s2; cin >> s1;
  int t, o, n; cin >> t;
  n = s1.size();

  // wrong idea
  for (int _ = 0; _ < t; ++_) {
    cin >> o >> s2;
  }
}

int main() {
  cin.tie(NULL);
  ios_base::sync_with_stdio(0);
  cout.tie(NULL);

  solve();
  return 0;
}