#include <bits/stdc++.h>
using namespace std;

void solve() {
  int n, k; cin >> n >> k;

  // basically do rounds
  // move an element up until you cannot and then go sequentially
  // if a round satisfy all then return the seq
  // if a round moves none return -1
  
}


int main() {
  cin.tie(0); cout.tie(0); ios_base::sync_with_stdio(0);
  
  int t; cin >> t;
  while (t--)
  solve();

  return 0;
}